# We cannot increase ebs volume using terraform ec2 module, can be done using ec2 resource.


To Run VPC module, need to install below commands on agent node.

terraform installation on agent node.
aws configure on agent node.
01-vpc uploaded in github

* We will run jenkins basics commands.

